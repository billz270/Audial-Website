# Audial — Project Documentation

This file documents the Audial website project. It's read by Claude Code at the start of every session so the AI has consistent context. Edit it freely as the project evolves — this is a living document.

---

## What this is

**Audial** is a custom acoustic-panel business based in Mumbai. The website lets customers design panels with their own artwork, plan panel layouts in their rooms, and place orders. Five static HTML pages, no backend (yet), and **no compilation step** — there is a minimal Netlify build, but its only job is deciding which files go public (see **Deployment**).

The brand name is Sanskrit — "human effort striking sound" — paired with the concept of *Nada* in Carnatic music. Five logo shapes = five fingers of a hand = five letters of the name.

Email: support@audial.in
Phone: +91 77180 49186
Location: Mumbai

---

## File structure

```
audial-website/
├── CLAUDE.md                  ← this file
├── netlify.toml               ← deploy allowlist — the ONLY thing deciding what is public (see Deployment)
├── index.html                 ← landing page, design-led hero
├── configurator.html          ← panel designer (3D viewer, image upload, transforms)
├── room-visualizer.html       ← wall layout planner with drag/marquee/snap
├── how-it-works.html          ← explainer page (acoustics myths above CTA)
├── about.html                 ← about/founder page
└── design-references/
    └── assets/
        ├── logos/
        │   ├── Audial Logo.png        ← logo mark only (2000×2000)
        │   └── Audial Logo_Black.png  ← wordmark only (2000×300)
        ├── old-logo/              ← archived original SVG files
        ├── website-icons/         ← ahata-v3-*.svg icon set
        ├── videos/                ← hero banner clips (DEV-39)
        │   ├── banner-1.mp4        ← 1080×1350 (4:5), 10.0s, 1.17MB
        │   ├── banner-2.mp4        ← 1080×1350 (4:5), 14.57s, 1.92MB
        │   └── banner-poster.jpg   ← frame 1 of banner-1, 11KB, load + failure fallback
        └── 3d-models/
            ├── panel-viewer.html   ← standalone Three.js panel viewer + cinematic assembly animation (DEV-31)
            ├── build-web-glb.mjs   ← strips Panels.glb master → Panels-web.glb (gltf-transform; DEV-32)
            ├── Panels.glb          ← ~59MB Blender master (gitignored, local only)
            └── Panels-web.glb      ← ~319KB web model (committed; DEV-32)
```

**`how-it-works.html` is the only page that loads 3D** (DEV-40 — the build viewer in the process section). The **configurator does not** — see "Parked: the 3D configurator" below. `build-web-glb.mjs` and `panel-viewer.html` remain build-only/standalone and are not referenced by any page.

The nav logo is composed of two `<img>` tags inside `.logo`: `Audial Logo.png` (mark, 50px height) + `Audial Logo_Black.png` (wordmark, 64px height), gap 8px. Nav auto-sizes to 64px. Adding a font or photo? Drop it in `assets/`, **add a `cp` line to `netlify.toml`** (see Deployment), and ask Claude to wire it up.

---

## Deployment

**Deploying is `git push`. There is no deploy command.** Netlify builds from GitHub `master` automatically, so a push to `master` is a public release. Never run `netlify deploy`, `npx vercel --prod`, or any other CLI publish. (The site ran on Vercel until 2026-09; `.vercelignore` is dead config kept only for reference — **Netlify does not read it**.)

**The project has a minimal build step, and its only purpose is file exclusion — never compilation.** `netlify.toml` runs a short `cp` script that assembles a `dist/` folder and publishes that. No bundler, no transpiler, no framework: the five HTML pages ship byte-identical to the repo. `dist/` is generated at build time and gitignored.

**Why it must work this way — there is no exclude list to write.** `netlify.toml` has *no* mechanism for excluding files from a publish directory. `[build] ignore` is a command that decides whether to **skip the build**, not a file filter; and a redirect rule that 404s a path doesn't remove the file (it stays on the CDN, and a real file beats the rule unless forced). The only reliable control is what lands in the publish directory — hence an allowlist.

**So the rule is inverted from `.gitignore`: if it isn't copied, it isn't public.** Anything sensitive stays private by being absent from the copy list; anything that must be public has to be added to it. Currently published: **11 files** — the 5 pages, the 2 logo PNGs, the 3 hero video assets, and `Panels-web.glb`. Deliberately withheld: `CLAUDE.md`, `TASKS.md`, `DESIGN.md`, `docs/superpowers/**` (14 files), 322 `design-references/` design-source files, the `3d-models/` build tooling, and `legal/*.docx` (founder's call 2026-09-13 — proper HTML policy pages are a future task).

**The one failure mode this creates:** a new image, video, font or model works locally and 404s in production. Add its `cp` line in the same change. The deploy log prints the full published file list — if something unexpected appears there, it is about to go public.

---

## Color tokens

```css
--ink:#093d53          /* navy primary — borders, text, dark surfaces, selection states */
--shadow:#00171f       /* near-black — drop shadow tints, modal backdrops only */
--paper:#f2f2f9        /* off-white background */
--primary:#093d53      /* same as ink (kept for semantic clarity) */
--primary-light:#007da6 /* teal blue — gradient companion to primary on hover states */
--accent:#e26167       /* peach-red — CTA fills, brand dot ◆, accent words */
--wood:#7A5F3A         /* lightest wood tone */
--wood-mid:#4A3622     /* mid wood */
--wood-dark:#2A1C10    /* dark wood */
--wood-edge:#1A0F08    /* darkest wood (gradient inner edge) */
--wrap-black:#0F0904   /* fabric wrap when full-wrap is selected */
```

**Usage rules:**
- Selection states (selected panel, active button) → `--ink` (navy)
- Button/tile hover backgrounds → `linear-gradient(135deg, var(--primary), var(--primary-light))` with `color:var(--paper)`
- CTA buttons and accent words → `--accent` (peach-red), used sparingly
- Text hover highlights (nav links, footer links, labels) → `--accent` (peach-red)
- Wood gradient is dark-heavy (mid stop at 60%, not 50%)

---

## Layout conventions

- `body { max-width: 1320px; margin: 0 auto }` — content centers on wide screens with paper-colored gutters
- All borders are `1.5px solid var(--ink)`
- Body font: `'Archivo'`. Headlines: `'Archivo Black'`.
- Letter-spacing: `-0.02em` to `-0.04em` on Black weights, `0.05em` to `0.15em` on uppercase

---

## Cart data model

Saved in `localStorage` as `acousticCart` (key name kept for back-compat with old saved sessions).

```js
{
  id: number,
  size: '1x1' | '2x1' | '2x2' | '4x2' | '1x4',
  baseW: number, baseH: number,            // canonical feet (immutable per size)
  orientation: 'horizontal' | 'vertical',
  effectiveW: number, effectiveH: number,  // rendered feet after orientation
  image: string,                           // data URL
  imagePosition: { x, y },                 // pixel offset in saved-panel space
  imageScale: number,                      // 0.5..2.0 user zoom
  imageNaturalWidth: number,
  imageNaturalHeight: number,
  flipH: boolean, flipV: boolean,
  rotate: 0 | 90 | 180 | 270,
  fileName: string, fileSize: number,
  woodVarnish: 'light' | 'dark',
  fabricWrap: 'half' | 'full',
  quantity: number,                        // user can buy 2+ identical panels
  savedPanelWidth: number,                 // configurator pixel size at save time
  savedPanelHeight: number,                //   → used to rescale image transform downstream
  thumbnail: string | null                 // DEV-37: posed JPEG of the finished panel (~7KB);
                                           //   null = custom size / no WebGL / pre-DEV-37 cart
                                           //   → both render sites fall back to the CSS render
}
```

Room plan saved as `acousticRoomPlan`:
```js
{
  room: { length, width, height },         // feet
  panels: [{ id, wall, x, y, w, h, ... }], // "..." = designedId, image, orientation, the full
                                           //   image-transform snapshot, and (DEV-45)
                                           //   woodVarnish + fabricWrap so the 3D view can
                                           //   paint the panel's edges without a cart lookup.
                                           //   Plans saved before DEV-45 lack the last two and
                                           //   fall back via designedId, then to light/half.
  furniture: [{ id, type, x, y, w, h, base, tall, wall, side }]   // DEV-47
}
```
**Furniture (DEV-47) is a sibling array, never mixed into `panels`** — no price, no quantity cap,
no artwork, and it reaches no statistic and never the checkout.
- `w`/`h` are **always** the floor footprint, for every type; the vertical is `base` (feet off
  the floor) + `tall`. A window's nominal `4×3` is therefore `w:4, tall:3, base:3`, with `h`
  pinned to `FURN_WALL_DEPTH` (0.35ft) so the Plan has a box to draw and hit-test.
- For a wall item (`window`, `door`) `x` is measured **along that wall**, in `r3dPanelQuad`'s
  editor-x convention — so the Plan, the 3D view and the 2D wall editor agree by construction.
- `side` is `'L'`/`'R'` on speakers only; the pair is independent after creation.
- Plans saved before DEV-47 have no `furniture` key and load with none.

---

## Critical functions

**`artTransformParts(panel, targetW, targetH)` (visualizer) — the shared numeric core**
Returns the raw numbers (`tx, ty, zoom, rotate, sx, sy, naturalW, naturalH`) rather than a
formatted string, because the three renderers that draw a saved design need different syntax:
CSS wants `px` units and `scaleX()`, an SVG transform attribute accepts neither. **Split out in
DEV-45 so the renderers cannot drift apart** — before that the 3D room view had its own (wrong)
copy that ignored every saved transform.

**`computeArtTransform(panel, targetW, targetH)` (visualizer)**
A thin CSS formatter over `artTransformParts`, output unchanged. Math:
1. `scale = min(targetW/savedW, targetH/savedH)` — uniform, never overflows
2. Final image transform: `translate(cssTx, cssTy) scale(zoom * scale) rotate(r) scaleX(±1) scaleY(±1)`
3. Bounding-box correction handles rotation/flip so the visible image stays inside the panel

**`panelToCartItem(p)` (configurator)**
Captures full design state. Always includes `savedPanelWidth/Height` so downstream renderers can scale properly.

**`loadDesignedCart()` (visualizer)**
Runs on initial load, on `pageshow` with `e.persisted === true` (bfcache restore), and on `storage` events (other-tab updates). Handles the sync bug where designed panels weren't appearing.

The configurator's `renderCartCardPreview` and the visualizer's `computeArtTransform` should stay in sync. Same math, different contexts.

**`r3dPanelArt` / `r3dPlaneHomography` (visualizer, 3D room view)**
The third consumer. Draws the design onto a panel's projected face using a CSS `matrix3d` inside
an SVG `foreignObject`, because **an SVG transform is affine and the projection of a wall is a
homography** — an affine map can only produce a parallelogram where perspective produces a
trapezoid. The homography is derived from the panel's *plane*, not from four projected corners,
so a panel partly behind the viewer still maps correctly. See the DEV-45 notes in TASKS.md before
touching it; affine strips were built, measured and rejected.

---

## Issues & Backlog

### High Priority
_(none)_

### Medium Priority
_(none)_

### Parked: the 3D configurator (DEV-32 … DEV-38) — REVERTED 2026-07-20

**The configurator no longer has a Three.js viewer. It is back on the CSS 3D preview, byte-identical to what is deployed on Vercel.** `configurator.html` and `room-visualizer.html` were restored to their `46df3da` state (verified: the restored `configurator.html` is 122,810 bytes, exactly what `www.audial.in` serves; the live site 404s on `Panels-web.glb` and contains no 3D markers — the 3D work was **never deployed**).

**Why:** the artwork cropped on the sides. The fabric front face is the panel's full *outer* face — `0.300 × feet + 0.055` world units on **both** axes — so its aspect is pulled toward square and never matches the nominal panel ratio (4×2 → **1.900**, not 2.000; 2×1 → 1.845; 1×4 → 3.529). Under the cover-fit that meant 5–12% of the art fell off the long axis. Separating the flat fabric front from the fold in Blender (done, and clean — see below) did **not** fix it, because the split changed no dimensions. The founder's call was that fixing it properly meant reworking the Blender master to non-physical "website" ratios, and that the 3D belongs elsewhere on the site rather than in the configurator.

**The plan going forward: upgrade the CSS preview using what the 3D work produced.** Concretely available:
- **Real wood textures** — `pine-wood-light` (41 KB) and `pine-wood-dark` (154 KB) JPEGs are inside `Panels-web.glb` and can be extracted to replace the CSS gradient fakes. Likely the biggest visual win.
- **True measured proportions** — the `0.300 × feet + 0.055` fabric-face geometry above; the CSS preview currently uses nominal feet.
- The `−35° / 8°` pose already matches what CSS uses; the 6-layer component breakdown is documented in the DEV-33 entry.

**Everything is recoverable.** The 8 commits (`15c124f` → `02c701b`) remain in `master`'s history and are tagged **`3d-configurator-archive`** (local tag; not pushed). `panel-viewer.html` is standalone, was never wired into the site, and is untouched — it's the natural starting point for rehoming the 3D. The detailed DEV-32…DEV-38 write-ups below are **kept deliberately**: they are the most valuable artifact of that work and describe the `.glb`, the build pipeline, and every trap hit along the way. Read them as *history and asset documentation*, **not** as a description of the current configurator.

**Two non-3D fixes were lost in the revert** (they were entangled in the 3D commits; re-apply if wanted):
1. **Finish section un-gating** — `customSection.style.display='block'` had been moved from `handleImageUpload` into `showDesigner()`. Reverted, so Wood + Side Wrap are once again hidden until an image is uploaded. That gate was never principled (clearing the art didn't re-hide them).
2. **`#imgReplaceBtn`** — a Replace button in the preview toolbar. Only needed because `panelFace` was hidden in 3D mode; the in-panel Replace works again in CSS mode.

**Blender master state (2026-07-20, current):** the fabric front is now its own flat 2-triangle quad per config (`<key> Acoustic Fabric`), fully separate from `<key> Half Fold` / `<key> Full Fold`. Verified: all 8 configs present, **zero fold triangles coplanar with the front plane** (no z-fighting, no art bleed onto the rim), every node name matches its mesh datablock name, no typos, all 8 Rockwool meshes carry the Rockwool material, both wood varnishes still in use so neither gets pruned. **`build-web-glb.mjs` has NOT been updated for this new node layout** — its `FABRIC_NODE_RE` still expects `<key> Acoustic Fabric (Half|Full) Fold`, so a rebuild against the current master will fail its guards (correctly, and loudly). Fix the regex before rebuilding.

### Recently resolved
- **Review-first order language (DEV-49)** ✓ (2026-09-14, committed locally, not pushed) — No customer-visible
  text suggests paying on the site. Openers read **"Place Order for Review →"**, the order modal's submit reads
  **"Submit Order Request"**, a review note sits above it in `#checkoutFooter2`, and success copy promises a
  print proof and payment link instead of delivery. Copy only; both Formspree modals untouched. **The two
  labels differ on purpose** — the spec assumed two competing buttons, but they are the opener and the step-2
  submit of one flow. **Internal names (`acousticCart`, `openCheckoutModal`, `.checkout-*`) intentionally
  still say cart/checkout** — do not rename the storage key. Full notes in TASKS.md.
- **Furniture in the room visualizer (DEV-47)** ✓ (closed 2026-09-11) — Seven line-art types: Window and
  Door mount on a wall, Desk/Chair/Speakers/Bed/Couch sit on the floor. **Added from a Furniture
  button on the view rail, in every view** (originally a sidebar section; see the follow-up at the
  end of this entry). Floor pieces are positioned in the Plan; windows and doors in the Plan or
  the 2D wall view. **The Edit Mode toggle was dropped**: a mode gating editing inside the editing
  view protects you from the thing you opened the view to do. **`w`/`h` are ALWAYS the floor
  footprint and the vertical lives in `base`/`tall`** — a window's `4×3` is width × height *up
  the wall* while a desk's `4×2` is a footprint, and `FP_DRAGGABLE`'s clamp reads `o.w`/`o.h` as
  floor-plane feet, so they could not share a meaning; storing plan-axis w/h and swapping them
  per wall was rejected as the exact bug class DEV-45 spent a task on. **`FP_DRAGGABLE` grew
  three OPTIONAL hooks** (`move`, `resize`, `readout`) rather than furniture growing its own
  handlers — a kind defining none behaves as before, so **DEV-46's ceiling drag coming out
  bit-identical is a regression CHECK, not an assumption**. Two registrations, because the
  constraints differ: floor pieces free-clamp; **windows and doors follow the pointer to the
  nearest wall and slide only along it, so crossing a corner RE-HOMES rather than detaching** —
  a window always belongs to some wall. Wall items are built by handing `r3dPanelQuad` a
  synthesized panel, so they inherit DEV-45's back/left mirroring free instead of by a second
  implementation. **3D furniture is unfilled and therefore NOT depth-sorted** — DEV-45's sort
  exists because filled panels paint over each other; with no fill the sort would run every frame
  to change nothing, and unfilled preserves DEV-43's x-ray read. **Three defects were caught only
  by screenshots, none by the 100 checks:** desk and chair drew the identical symbol (the
  pile-of-crates failure the "recognizable outlines" call exists to prevent — the chair gained an
  inset seat, the bed a pillow rect), and BOTH the Plan and 3D empty-state overlays told a
  furnished room it was empty, because both counted `state.panels` only. **Wall furniture carries
  no Plan label** — its 0.35ft-deep footprint sits exactly where the wall's own name is painted,
  worst at 390px; dashed reads as a window, a swing arc as a door. The **swing arc is Plan-only**
  (a swing is floor sweep; `cross(a,n)` is +1 on all four walls, so the sweep flag is a constant,
  not a per-wall case). **The Plan edits only a wall item's position and width** — a top view
  cannot show height up a wall, the same reason DEV-46 refused Plan-dragging of wall panels.
  **Height and sill are edited in the 2D wall view**, which can (follow-up). **Overlap is allowed with no collision detection**, deliberately. **Pitch finding — RESOLVED, the
  tilt stays locked at level** (founder, 2026-09-11): furniture now sits below the eyeline, and
  facing the front wall of a 14×12 room the couch reads 37% in frame and the bed 1% at level,
  versus 100% and 16% at −20°, but the founder does not want furniture to take the focus.
  **Follow-up (2026-09-11, `f1ad35b`, `eef74bf`):** the sidebar picker became a **Furniture
  button on the view rail** (after a hatched gap; navy when open, because it is an action, not a
  view) opening `#furnTray` over the viewer — a sibling of the panes, never inside the
  pointer-capturing stages. Plan: all seven, room centre. 2D: Window/Door only, onto the wall in
  view, with floor items **hidden** from the 2D tray (`FURN_2D_FLOOR = 'hide'`, founder's call
  2026-09-11; `'grey'` would show them disabled instead); disabled on the ceiling. 3D: wall items on the wall faced; floor items via **`furn3DLanding()`, because with the
  tilt locked the floor at the room centre is below the frame** — it walks ahead until the
  piece's near floor edge is in view. Windows and doors are now **editable in 2D**: drag (windows
  also up/down for the sill; doors stay on the floor), `w`/`e`/`n` handles plus a window-only `s`
  sill handle, × or Delete. They draw **behind** panels (a press where they overlap is the
  panel's) and are pointer-transparent while a panel size is armed. In 2D a window is a **solid
  2.5px line** (founder's call; the Plan keeps its dashed architectural symbol). **Dragging
  furniture in 3D is PARKED.** Screenshots again caught two defects no assertion did (the
  empty-wall prompt printed across a see-through window; a window label sat under its sill
  handle). Full record in TASKS.md. (`room-visualizer.html`)

- **Camera pitch in the 3D room view (DEV-48)** ✓ IN REVIEW — The 3D camera can tilt, so ceiling panels read down the room the way wall panels read across it. **The founder's premise was half wrong and the measurement is the useful artifact: the side walls were never doing better.** Matched 2×2 panels on the left wall and on the ceiling at the same depths into a 14×12×10 room both hit 100% at 0–4ft and **0–4% at 4–6ft** — both surfaces die ~4ft into a 12ft room. The ceiling just made the limit visible, being the first content above the eyeline. **Two cheaper fixes were computed and rejected:** widening the FOV needs ~86–92° (from 70) and partly cancels itself because `r3dEyeOffset` derives the backoff from the FOV; raising the eye to 6.6ft reaches mid-room ceiling but collapses the visible floor to 1.4ft — and the floor is where DEV-47's furniture goes. **Implementation:** `r3dCamera(yaw, eye, pitch)` builds a real orthonormal basis (`r` horizontal, roll always zero; `u = r × f`), which **at pitch 0 evaluates to exactly (0,1,0), so the flat view is bit-identical — verified, not assumed** (same ceiling-panel bbox to the pixel). **The back-off uses the HORIZONTAL forward, never the pitched one** — tilting your head doesn't move your eye, and backing off along the view direction would slide the eye down the room and make it breathe. **`r3dPlaneHomography` carried the same assumption buried in it** (`U.y`/`V.y`/`O.y` for screen-up, under a comment saying pitch is always zero) and twists every panel's artwork under tilt if left alone; it now reads all three from `cam.u`. `r3dTick` eases both axes and **both must settle before the loop stops**, or a tilt still easing freezes whenever yaw arrives first. **`R3D_PITCH_LIMIT` is exactly `R3D_FOV/2` (35°) upward: at that tilt the horizon sits precisely on the frame edge**, so it never leaves view and the room can't be lost — a limit with a reason. **`R3D_PITCH_MIN` is 0 — the view never tilts DOWN** (founder's call on review): nothing is ever placed below the eyeline, so a downward tilt only shows bare floor, and level is the bottom of the range, exactly where the view was locked before this task. Raised once that DEV-47's furniture sits on the floor and overridden; reopening it is that one constant. **Gesture is split by pointer type, not by width:** a vertical drag tilts only when `e.pointerType !== 'touch'`, so touch keeps `touch-action:pan-y` and a visitor can still scroll past a viewer in a long page (the same constraint DEV-40 and DEV-41 hit); touch gets two tilt buttons instead, 12° per tap, which must `stopPropagation` on `pointerdown` or the tap also starts a 0px view drag. Ceiling reach, facing the front wall in a 12ft room: flat `4ft=0%`; tilted 25° `6ft=100%, 8ft=2%`; at the limit `8ft=44%`. The wall snap stays yaw-only. 23 checks green plus DEV-46's suites unchanged. (`room-visualizer.html`)
- **Ceiling panel placement (DEV-46)** ✓ IN REVIEW — Ceiling clouds can now be created, seen and moved. **The ceiling is just another `wall` value**, not a second array: `WALL_LABELS.ceiling` plus a `getWallDimensionsFt` branch returning `roomLength × roomWidth` made it a legal surface, and placement, drag, marquee select, snapping, alignment arrows, delete, the "+ Add Design" popup, the quantity cap, Clear All, stats and save/load all came along free — every one of them was already written against `state.panels` + `getWallDimensionsFt(state.activeWall)`. **`WALL_ORDER` stays the four vertical walls and `SURFACE_ORDER` is the list that includes the ceiling**; the 3D opacity/focus/yaw tables are wall-only, so a ceiling in `WALL_ORDER` would have had `r3dFocusedWall()` try to face it. **Orientation is PLAN view, not looking-up**: top edge = front of room, so editor `(x,y)` → world `(x, z=y)`, identical to the Floor Plan, and nothing mirrors; the four edges are labelled inside the surface. Four things genuinely needed writing: the thumb strip went 3→4 (2×2 below 560px); the `← Left Wall / Right Wall →` labels read `WALL_ADJACENTS[activeWall]`, **undefined for the ceiling**, so they and the floor strip hide under `.ceiling-mode`; edge-drag resize means WIDTH/LENGTH on the ceiling rather than HEIGHT, so the axis choice (inlined in three places) became `surfaceResizeAxis(edge)`; and `r3dCeilingQuad` + the `state.ceilingPanels` loop were **deleted** in favour of a `ceiling` case in `r3dPanelQuad`, after which the DEV-45 extrusion, depth sort, finish and artwork pipeline ran on ceiling panels unchanged — code that had never once executed. **`R3D_ART_CORNERS.ceiling` turned out correct**, but was verified by screenshot against the 2D surface with the four-quadrant test artwork, never by assertion (DEV-45's mirrored back/left walls passed every assertion written about them). **The Plan's drag is generic (`FP_DRAGGABLE`)** — a kind registers a `find` and a `commit`, so DEV-47's furniture is a registration; **pointer capture is taken on the STAGE, not the rect**, because every move re-renders the SVG and destroys the element under the pointer. Wall panels stay non-draggable in the Plan, deliberately. **Coverage now divides by wall + ceiling area** (14×12×10 room: 520 → 688 sqft, so an existing 10.0% redraws as 7.6%), and the Plan's wall names are painted last with a `--paper` halo so a ceiling panel can't swallow them. **Open finding, not a defect:** DEV-43's camera is yaw-only with zero pitch, so in 3D only ceiling panels near the wall you face are visible — measured, a 4×2 at 4ft into a 12ft room projects entirely above the frame. 55 checks green in Chromium at 1440×900 and 390×844. (`room-visualizer.html`)
- **Correct artwork, panel depth, and wall snap in the 3D room view (DEV-45)** ✓ — Closes DEV-43's open artwork glitch and gives placed panels physical presence. **The glitch was that the 3D view ignored every saved image transform**: `r3dPanelArt` drew the raw image into a 1×1 unit square with `preserveAspectRatio="none"`, so `imagePosition`/`imageScale`/`rotate`/`flipH`/`flipV` were all discarded — the founder described it exactly as "the entire image loads in the centre with some slight warping". Fixed by splitting `computeArtTransform` into `artTransformParts` (numbers) + a CSS formatter, so the 2D wall, the cards and the 3D view share one core. **The deeper finding: AN SVG TRANSFORM CANNOT EXPRESS A HOMOGRAPHY.** A wall at an angle projects to a trapezoid and an affine map can only make a parallelogram, so mapping art through three corners twists it against its own panel — **measured 9% of the panel diagonal and 5.8° of edge twist at 30° of yaw, 19% and 12° at 60°**. (DEV-43's comment calling this "imperceptible at panel scale" was wrong.) **Affine vertical strips were built, measured and REJECTED — do not retry:** they cut the error to 0.65% but cannot be made continuous, because an affine map matches both vertical edges of a strip only when their projected heights are equal, which perspective guarantees they never are; every boundary became a visible break in any straight line. **Centring each strip's fit made it worse** — lower peak error, but the discontinuity spread across both edges instead of one. The fix is a CSS `matrix3d` inside a `foreignObject` (CSS does the perspective divide *after* the matrix, exactly what a homography needs, and `foreignObject` keeps it in the SVG's paint order): **worst error 0.0004px at every angle to 75°, versus 55px before — exact, not close.** It is also *cheaper*: 24 SVG nodes instead of 256, ~1ms per render vs 5–9ms. **The homography is derived from the PLANE, not projected corners**, so a panel crossing the near plane keeps its art — 31 such cases across a full turn, all previously flat accent fills (they sit at the extreme frame edge, so the gain is peripheral). **Separately found: art was MIRRORED on the back and left walls** — `r3dPanelQuad` mirrors those walls so panels land correctly, which also swaps which vertex is the panel's top-left (+x pointed 83px *left*); `R3D_ART_CORNERS` now holds the per-wall order, and **`ceiling` in it is an unverified copy of `front`**. **Depth:** panels stand `R3D_PANEL_DEPTH` (2.7in, the real thickness) off the wall; visible side faces are chosen by **world-space facing, not projected winding** (outward normal = box-centre→face-centre with the wall-normal component removed), so a panel to your left correctly shows its right edge head-on; faces take the panel's real varnish, or `--wrap-black` for full wrap, resolved snapshot → `designedId` lookup → light/half default. Panels are depth-sorted now that side faces are opaque. **Snap:** releasing a pan within 13° of a wall eases square (nearest multiple of 90°, since yaw is unclamped — verified at −445°); a release-time threshold, **deliberately not a detent**, which would fight the hand. **Snap easing was tuned in FRAMES: a headless browser does not run rAF at 60fps**, so the first "476ms" measurement was really 59 frames ≈ 1s and sluggish; 0.13 is visually complete in 22 frames (~370ms). Dragging now anchors to the *visible* yaw, not the target, so grabbing mid-ease no longer jumps. **Traps:** `clip-path` resolves post-transform (clip `<g>` and matrix `<g>` must stay separate); digit runs inside artwork data URIs are not coordinates (a NaN scan reported 197 false failures — strip `href`/`src` first); puppeteer element screenshots resize the viewport and leave a hidden view pane at zero size, which reads exactly like a 2D regression; reading state from outside the page races input events (a touch test reported a 20° snap that was really 6°). **Verified in real Chromium *and* Firefox** (3D transforms in `foreignObject` are a historic Firefox weak spot; identical), 53 checks across three suites. (`room-visualizer.html`)

- **3D line-art room view in the visualizer (DEV-43)** ✓ — A new view showing the room as line-art with depth, built in **plain SVG with no Three.js** (~200 lines of projection maths writing `<polygon>`s; a WebGL context would have been pure weight). Went through four rounds of founder feedback that each changed the fundamentals, so **read its TASKS.md notes bottom-up**: the camera started *orbiting outside* the room, then framing was pulled closer, then the camera **moved INSIDE the room and turns in place** (superseding the orbit model entirely — near-plane clipping removes the wall behind you, so no fade or blur is needed there), then the ±90° yaw clamp was removed so any wall including the back can be faced. **The eye cannot sit at the exact centre**: from the centre of a 14×12×10 room the front wall subtends 119% of the frame, so the eye backs off until the wall fills ~2/3, then clamps to stay inside for *every* wall. **Surfaces are drawn as four independently clipped EDGES, never a closed polygon** — closing a near-clipped polygon draws a spurious edge straight across the view. **The camera fit was silently broken and non-convergent** (FOV 70 produced a 433ft orbit radius); replaced with bisection, which is exact because projected extent is monotonic in radius. **The fit sampling MUST include the diagonal** — sampling 0/30/60/90 skips 45°, the widest view of a box, and every automated check passed while the room visibly overflowed there. Wall coordinate mapping is mirrored per wall and was **derived, not guessed** (front maps straight, back mirrors, left mirrors along depth, right maps depth straight). No occlusion culling, deliberately — the x-ray read is what "line-art + opacity fading" produces. `touch-action:pan-y` so horizontal swipes orbit and vertical ones scroll the page. The rAF loop stops when the easing settles, so an idle view costs nothing. **Two founder overrides not to "fix" back: no yaw clamp, and the active rail button is `--accent`** (contradicting the "selection states → --ink" rule). (`room-visualizer.html`)

- **View-mode rail in the room visualizer (DEV-42)** ✓ — Left-side function panel with 2D / 3D / Floor Plan switching, the architectural foundation for DEV-43 and DEV-44. Only 2D was functional; the other two showed placeholders. `state.panels` was already the single source all three views read. **A `<nav>` cascade spread the rail buttons down the whole column** — caught by a screenshot, not by any assertion. (`room-visualizer.html`)

- **Selected-layer glow + auto-rotate + mobile one-finger controls (DEV-41)** ✓ — The How It Works build viewer now rims the selected layer in `--accent` via an `EffectComposer` → `RenderPass` → `OutlinePass` → `OutputPass` chain, and swings the camera to the layer's side. **The outline colour needed far more than a constant:** `OutlinePass` composites **additively**, which can only *brighten*, so peach over `--paper` washed out to `rgb(255,156,159)` with red clipped — and lowering strength/glow made it **paler, not more saturated**. Fixed by overriding `overlayMaterial` to a **premultiplied alpha composite** (`CustomBlending`, `One / OneMinusSrcAlpha`): the edge texture already stores `rgb = edgeColor*d, a = d`, so `rgb/a` recovers the pure token and lays it at full saturation on any background. **The hex in the code is `0xcc4f56`, NOT `#e26167`, deliberately** — the rim composites *before* `OutputPass`, so ACES tone mapping pushes a literal `#e26167` out to `rgb(237,126,127)`; `0xcc4f56` renders as `rgb(227,96,101)` vs the token's `rgb(226,97,103)`. **Measured, not derived — re-measure if tone mapping changes.** `OUTLINE_GLOW` is **0**: once colour is normalised to full saturation the blur texture stops being a halo and becomes a soft pink cloud that swallows the rim. **The ×6 edge gain must sit INSIDE the clamp with `edgeStrength` applied after** (`clamp(mask.r*edge.a*6.0,0,1) * edgeStrength`) — folding `edgeStrength` in makes the rim opaque at 0.167, so the 200ms fade finishes in ~33ms and pops. Also: **the alpha clamp is load-bearing** — the composer target is HalfFloat, so an alpha >1 makes `(1-srcAlpha)` negative and punches a hole in the frame. **Auto-rotate** tweens in *spherical* space around the orbit target (shortest-path azimuth, 600ms ease-in-out-cubic) so the path arcs around the panel; `BACK_DIR` is `HOME_DIR` mirrored through Y so front and back read as the same shot. Damping is disabled for the tween's duration or OrbitControls' decaying `sphericalDelta` gets added on top every frame. Core layers (`Frame`, `Rockwool`) map to `null` in `LAYER_SIDE` and deliberately do **not** move. `fitCameraTo` calls `endCamTween()` so a re-frame supersedes a rotation in flight, and `controls`' `start` event cancels it so a drag always wins. **Part 3 splits the gesture by AXIS, not finger count:** `touches.ONE = ROTATE` + `touch-action:pan-y` → horizontal drags orbit, vertical drags go to page scroll (full single-finger orbit needs `touch-action:none`, which traps a viewer embedded in a long page). `touches.TWO` is `DOLLY_ROTATE` not the spec's `DOLLY_PAN`, since panning is off. **Verified 20/20 in real Chromium *and* Firefox at 1440×900 and 390×844:** rim identical `rgb(227,96,101)` in all four, back layers rotate (net 5.686), core layers hold at exactly `0.0000`, zero console errors. **Testing trap: element screenshots cost ~1.5s each, so a 600ms tween CANNOT be verified by diffing them** — it reported "0 px changed", which reads exactly like a broken tween; inject a per-frame recorder into the rAF loop via `page.route()` instead. **Firefox RDM cannot show Part 3 at all** unless touch simulation is on — it only resizes the viewport, so drags stay mouse events. Cosmetic nit left alone: at DPR 3 the rim shows faint dashed breaks on near-vertical edges (`OUTLINE_THICKNESS` 2.0→3.0 smooths it, chunkier). (`how-it-works.html`)

- **3D build viewer on How It Works (DEV-40)** ✓ — The process section is now a 4×2 step grid (left) driving a Three.js panel viewer (right). **The founder's call was to REPLACE the horizontal timeline, not add beside it** — the spec said "do NOT replace the existing 8-step content," but the new section sits directly above `.benefits`, exactly where `.process` already was, so building it as written would have printed the same eight steps twice ~200px apart. `#processScroll`, `#processDots`, the scroll-snap CSS and the dot-tracking JS are gone; **the 8 SVG icons, step titles, numbers and the line-draw reveal are kept byte-for-byte** (founder: rearrange, don't replace). Cards are number + icon + title — **no descriptions**, since none existed on the page and inventing 8 lines of copy wasn't wanted. **Layer mapping verified against the real titles:** 02 Create Frame→`Frame`, 03 Install Rockwool→`Rockwool`, 04 Fiberglass Backing→`Fiberglass Sheet`, 05 Back Support→`Back Support`, 06 Fiberglass Screen→`Fiberglass Screen`, 07 Acoustic Cloth→`Acoustic Fabric`; 01/08 are hover-only. Selecting dims the other five to **0.35 with `depthWrite:false`** — depthTest stays on so the selected layer still occludes, but dimmed layers must not depth-write or they occlude each other by draw order. **Column balance is `align-items:stretch` + `grid-auto-rows:1fr`**: the step grid takes the viewer's exact height, so the two columns always bottom out together and cards breathe at any width (a fixed-height grid left ~200px of dead space). **The camera fit had to become iterative.** A closed-form distance from the bounding box UNDER-shoots — viewed at 38° yaw the panel's near edge sits closer than the fit distance and projects larger, clipping top and bottom (the same asymmetric-projection trap as DEV-37). It now projects the 8 box corners and rescales distance until the worst corner lands inside, converging in 2–3 passes. A bounding-**sphere** fit was tried first and works but wastes ~15% of the frame on elongated configs. `FIT_MARGIN` 1.12, FOV **30** (35 made wide configs read as a wedge). **Viewer has its own 5 size buttons** (1×1, 2×1, 2×2, 4×2 default `4x2V`, 1×4); selection survives a size change. **Wood is force-unified to `Pine Wood Light`** across all configs — the `.glb`'s per-config varnish split is a prune artifact (DEV-35), and without this the wood visibly changed colour as sizes switched; cloned per mesh so layer dimming stays independent. **Mobile deliberately uses two-finger rotate** (`touches.ONE = NONE`) — one finger must stay free to scroll past a viewer embedded in a long page — and **OrbitControls writes `touch-action:none` INLINE, beating the stylesheet**, so the `pan-y` override must be set on `canvas.style`. Tapping a step scroll-the-viewer-into-view on mobile, since the 2×4 grid pushes it off screen. Clicks inside `.build-viewer` are excluded from the outside-click reset or every orbit drag would clear the selection. **Fallback = steps only** (no static render exists): no WebGL *or* model load failure → `.process.no-3d` hides the viewer, widens the grid, strips badges/roles, hides the 3D-specific copy; verified by forcing both. **Verified in real Chrome** (puppeteer, 52 checks + 16 fallback checks, all green): every size fits with no clipping at 1440×900 and 390×844, selection/reset/toggle semantics, orbit-preserves-selection, no console errors beyond the pre-existing `favicon.ico` 404. **Two harness traps worth remembering:** `drawImage` on the WebGL canvas returns a transparent buffer (no `preserveDrawingBuffer`) — screenshot the composited page instead; and `page.screenshot({clip})` is **document**-relative while `getBoundingClientRect` is **viewport**-relative, so once the page scrolled it silently measured section borders elsewhere and reported false clipping — use an element handle. (`how-it-works.html`)

- **Hero video banner replaces both slides (DEV-39)** ✓ — `index.html`'s hero right column is now two 4:5 clips crossfading in infinite rotation. **The founder's call was to remove *both* DES-9 slides**, not just the showcase grid the task named: the line-art configs, the 12-panel `.showcase`, the 2 slide dots and `goHeroSlide()` are all gone (51,677 → ~28,300 bytes) and the video pair does the cycling. **The videos needed no processing** — already 1080×1350 = exactly 4:5, h264/30fps, 1.17 MB / 1.92 MB, so the spec's 5 MB compression step was moot; `banner-poster.jpg` is ffmpeg frame 1 of banner-1 (11 KB). **`.hero-video` is `position:absolute` and that is load-bearing** — it contributes zero intrinsic height, so the hero row stays sized by `.hero-left` exactly as the slider did (verified pixel-identical to the live baseline: heroH/ctaTop/procTop deltas all 0.0 at 1920→940). A first attempt with `flex:1` + `aspect-ratio` created a cyclic size dependency and let the video drive the row to 725 px. **`width:min(100%,80cqh)` is also load-bearing:** height-locking the box made `max-width` clamp the width at 1024 px, breaking the ratio to 0.73 and cropping ~9% of the clip under `object-fit:cover`; container-query units let whichever axis runs out first cap the box, so it is exact 4:5 from 320 → 1920 px. `.hero-right` therefore has `container-type:size`, and **the mobile block must reset it to `normal`** — `container-type:size` with `height:auto` collapses the stacked column to zero. Mobile is full-width but `max-width:56vh` caps the clip at 70vh (an iPad-portrait hero grew +450 px without it); the hero still grows below 900 px (+142 px on an SE, +222 px on iPad Air) since a 4:5 portrait clip replaces a 1.4 landscape one. **Preload:** video 1 `preload="auto"`, video 2 `preload="none"` until a `timeupdate` arms it 3 s from the end (measured firing at 7.3 s) — *fewer* bytes on load than the spec's `metadata`-on-both. Setting `preload='auto'` alone starts the Chromium fetch, so the `load()` nudge is deferred 1 s and guarded on `networkState !== 2`; calling it eagerly cost a **duplicate 1.9 MB request**. **Three things the spec omitted but that are mandatory:** `playsinline` (iOS Safari forces fullscreen without it), `muted` as both attribute *and* property (Safari checks the property before allowing autoplay), and a `.catch()` on every `play()`. The `poster` attribute is **not** a real fallback — it only covers initial load — so a sibling `<img>` layer plus an `error` handler restores it at any point (verified by aborting both sources: poster shows, hero height unchanged). **Testing trap:** `python -m http.server` ignores HTTP Range requests, so `video.currentTime = dur-1` **silently seeks back to 0** and the clip restarts — the first rotation test read exactly like a broken `ended` handler but was the server. Re-verified with real-time playback: handoffs at t=10s/24s/34s/48s/58s, plus a mobile run, zero console errors. (`index.html`, `design-references/assets/videos/`)

- **Mobile performance testing (DEV-38)** ✓ — **The outcome is that no code was needed.** Measured in real Chrome at a Pixel-7-class viewport (390×844 @ DPR 3, `isMobile`+`hasTouch`) with real CDP touch events and CPU throttling — **emulated, not hardware**; the phone GPU is unmeasured, judged negligible at ~1010 tris with DPR already capped at 2. Results: **page load 350 ms**, 3D viewer ready **~410 ms**, three.js from unpkg ~190 ms, `.glb` 312 KB, JS heap 7.4 MB. Orbiting at **6× CPU throttle** (mid-range-Android proxy) was **vsync-capped with zero long frames**; it only degrades at **20×** (median 33 fps — a fictional device). **Decision: 3D stays default on mobile — no "View in 3D" toggle, no static fallback**, since the spec's own rule was "if performance is acceptable, leave 3D as default". **Touch controls needed zero work** — OrbitControls gives one-finger rotate + pinch-zoom natively (verified: camera moved; pinch took distance 4.39 → 2.71), `enablePan` already off, distances clamped. **Lazy-loading three.js/`.glb` was rejected on measurement:** it would save a few hundred ms of page load in exchange for a slower *first 3D paint* — bad trade. **The continuous render loop stays** — it's a battery cost, not a frame-rate problem (never dropped a frame at 6×), and DEV-35's varnish swap + DEV-36's fold toggle both depend on it landing next frame. **Measurement trap:** timing `page.goto` with a harness stopwatch reported **8 s**, flat across throttling — which reads exactly like a network-bound load, but was Chrome's cold-start overhead; the page's own `performance` navigation timing says 350 ms. **Read navigation timing, never time `page.goto`.** Also: unpkg omits `Timing-Allow-Origin`, so cross-origin `transferSize` reads **0 bytes** — not evidence of an empty fetch. Only console error is the pre-existing `favicon.ico` 404. (no files changed)

- **`build-web-glb.mjs` hardening (pre-DEV-38)** ✓ — Fabric folds were matched by a hardcoded material-name list (`ARTWORK_MATERIALS`) that included the `Arrtwork 3` typo, so **renaming that material in Blender would have silently shipped an 11.57 MB model instead of 319 KB** — measured against a renamed master: only **14 of 16** prims reassigned, the 2×2 folds keep their art material, `prune` sees a live user, and the 11.25 MB texture rides along, with no error. Fabric is now found by **node name** (`^(.+) Acoustic Fabric (Half|Full) Fold$`), making material names irrelevant. **Provably equivalent:** the master has exactly 16 fabric nodes (8 configs × 2 folds, 1 prim each) carrying exactly the 5 materials the old list named, and the rebuild is **byte-identical to the committed model** (same SHA-256, 319 KB). Guards added, **each verified by forcing its failure**: no fabric match / a config missing a Half or Full fold → throw; either wood varnish pruned → throw (DEV-35's toggle needs both); output over `MAX_WEB_BYTES` (1.5 MB) → throw. Writes to a **temp `.glb`, validates, then replaces**, so a failed build leaves the good model intact (confirmed). **Trap:** `NodeIO` picks format from the **extension** — a temp path of `Panels-web.glb.tmp` made gltf-transform emit a JSON glTF with loose `baseColor_*` sidecars that then got renamed over the real `.glb`; caught only by hash-comparing against the committed baseline. **Temp GLB paths must end in `.glb`.** (`design-references/assets/3d-models/build-web-glb.mjs`)

- **Blender asset cleanup + rebuild** ✓ (commit `1bfd3ed`) — With the hardening in place the master was re-exported: `Arrtwork 3` → `Artwork 3`, `Bacdrop` → `Backdrop`, and the `Fiberglass Screen` mesh datablock names tidied (they'd drifted to `4x2H ... .001/.002`; harmless, since the runtime reads **node** names first and strips `.001` suffixes). **The find that mattered wasn't a typo:** a full scan of every name in the master — prompted by the founder asking "any other typos?" — showed **5 of 8 Rockwool meshes had no material at all** (`1x1`, `1x4H`, `1x4V`, `2x2`, `4x2H`; only `2x1H`/`2x1V`/`4x2V` had it). Those five rendered with glTF's default material whenever a customer toggled Rockwool on in DEV-33's Components drawer. **Now 8/8.** Worth re-running that whole-file name scan after any Blender re-export. The material renames were a **no-op for the web build by design** — fabric is matched by node name, so spelling is irrelevant. Rebuild verified: 319 KB, 16 prims across 8 configs, texture bytes unchanged at **235,835**, both woods survived prune, no artwork material leaked, all 8 configs × 6 components classify with zero unclassified nodes. Verified in real Chrome too: `1x1`/`2x2`/`4x2H` all report the Rockwool material with all 6 components, varnish drives Light → Dark, wrap drives half → full, Rockwool layer toggle hides/shows. (`Panels-web.glb`)

- **Cart thumbnails via canvas screenshot (DEV-37)** ✓ — Cart cards now show a static 3D render of the finished panel. **The bug was worse than "no thumbnail":** cart cards carried `woodClass`/`wrapClass` on a `.cart-3d` div, but every rule for `.wood-dark-scene`/`.wrap-*-scene` selects `.panel-side` — and cart cards contain only `.cart-face` + the artwork `<img>`, no wood strips. **Dead code, so the varnish and wrap the user picked were invisible in the cart**; the card was bare artwork tilted 14°. **`captureThumbnail()` is synchronous by necessity** — the renderer has no `preserveDrawingBuffer`, so `toDataURL` must run in the same block as `render()` before the browser composites and clears the buffer (upside: no paint mid-function, so the temporary 400×400 frame is never visible). **It normalizes before capturing, and that isn't optional:** DEV-33's drawer can hide layers, and a naive screenshot would store a *skeleton panel* — so it forces every `layerState` row on, clears `frontHover`, captures, then restores exhaustively. **JPEG-on-paper was free:** `scene.background` is already `0xf2f2f9`, the cart card's own background, so transparency renders nothing and the spec's PNG would have cost ~100–300KB/item for zero gain — measured **~7KB/thumbnail, 60KB for a 4-panel cart**, and the localStorage anxiety was unfounded. **Sizing matches the CSS cards:** `fitThumbCamera` frames each config to `sqrt(area/8) * 0.95` of the frame (`THUMB_AREA_SQFT`/`THUMB_FIT`) — `renderCartCardPreview`'s own `CARD_MAX_LINEAR * sqrt(area/CARD_MAX_AREA)` curve. Every size lands within 3% of its old CSS size (4×2 → 160px exactly). **A single fixed distance was built first and FAILED — don't retry it:** the square frame must fit the tallest panel (1×4V), and `object-fit:contain` then shrank everything into the card's 160px box, rendering horizontal panels at **~60% of the CSS size** (4×2 at 96px vs 160px). A 4ft *vertical* panel isn't foreshortened by the 35° yaw while a 4ft *horizontal* one is (×cos35), so 1×4 already sat at 96% and blocked any uniform zoom. Matching CSS therefore means **abandoning physical proportionality** — the CSS `sqrt(area)` curve was never physically true (1×1:4×2 = 2.83 vs the physical 3.54); founder's call, made with measurements in hand. **The fit must RECENTRE, not just scale:** under perspective a wide panel at 35° yaw projects *asymmetrically* (near end projects further out), so scaling alone let the 4×2 span 95% of the frame while hanging off the right edge; `fitThumbCamera` corrects distance **and** pan each pass, panning eye and target together to preserve the view direction. **The `.glb` is not proportional to feet** — frame long sides are 1×1→0.354, 2×2→0.654, 4×2→1.254 units, i.e. `0.30 × feet + 0.054`, where **+0.054 is the wood frame thickness**; never assume 1:2:4. **The azimuth sign had to be measured:** CSS is Y-down/left-handed and the model carries a baked 180° Y rotation; `+35`/`+8` proved correct, confirmed by rendering beside the real CSS preview and checking the artwork wasn't swapped left-for-right (the tell a mirror leaves). **Verification trap:** a pixel-scan clip test at ±8 of the paper background reports false clipping — JPEG chroma ringing bleeds ~10px past the panel edge; use ±25 (panel colours differ by 40+). **Fallbacks:** returns `null` for custom sizes (no `SIZE_TO_CONFIG` entry) and no-WebGL; both render sites branch on `panel.thumbnail` and fall through to the pre-existing CSS/flat-art markup, which also covers carts saved before this change (no migration). Visualizer wall panels stay flat head-on art. **Cosmetic limit:** at −35°/8° the frame reads as a thin sliver — faithful to the CSS angle, which is equally thin; ~45–50° would show more frame but stop matching CSS. (`configurator.html`, `room-visualizer.html`)

- **Fabric wrap toggle drives the 3D viewer (DEV-36)** ✓ — The Half/Full Side Wrap buttons now drive the Three.js model. **Pure wiring — no Blender work:** the `.glb` already ships both wraps per config as *real geometry* (`"<key> Acoustic Fabric Half Fold"` / `"... Full Fold"`), already classified into `frontMeshes[key][half|full]` + `foldMeshes[key][half|full]`, and `applyFabricFold()` already promoted the active fold's front mesh into the `Acoustic Fabric` slot and hid the inactive variant. What was missing: `currentFold` was hard-coded and **never reassigned**, and the wrap buttons only toggled the CSS scene classes. **The default was an active lie:** `currentFold='full'` while `freshPanel()` sets `fabricWrap:'half'`, the Half button ships `active`, and line ~1788 adds `wrap-half-scene` — so every first render showed Full while all UI said Half. `currentFold` is now `'half'`, agreeing with the data model (founder's call). **`setFold` needs strictly more than `setVarnish`:** varnish rides the continuous render loop because reassigning `mesh.material` lands next frame, but a fold change is a **mesh swap** — `applyFabricFold()` only *hides* the outgoing variant and re-points the slot; the incoming mesh is still `visible=false` and carries its own **cloned, unpainted** material. So `setFold` also calls `applyFabricVisibility(key)` + `renderFrontFaceCanvas()`, or the toggle shows nothing / drops the artwork. **Deliberate deviation from the task spec:** the spec said re-run `showConfig()`, but that also calls `centerCameraOn()` — every wrap toggle would snap the camera home and discard the user's orbit, contradicting the "instant, matching component-toggle feel" spec. Fabric visibility was instead split out of `showConfig` into **`applyFabricVisibility(key)`**, shared by both; `showConfig` is otherwise untouched so size/orientation changes still re-frame (correct there). Seeded at the **`showForSize` choke point** beside DEV-35's varnish seed, so an edited saved panel restores its wrap structurally. **Verified in real Chrome against the real `.glb`** (18/18, live mesh state via `window.__dev32`): default Half on button *and* mesh, both toggle directions across all 8 configs, camera preserved on wrap toggle (with a control proving size change still re-frames), edit-restore, and artwork surviving toggles both ways. **Probe trap:** `window.__dev32.camera = camera` snapshots `undefined` — `camera` is declared at module top but only *assigned* in `init()`; the existing `() => ...` probes work only because closures read the live binding. Now a `defineProperty` getter; new `foldProbe`/`camProbe`. (`configurator.html`)

- **Wood varnish toggle in the 3D viewer (DEV-35)** ✓ — The Light/Dark buttons now drive the Three.js model. **They previously only toggled `wood-dark-scene` on the hidden CSS preview**, so the 3D panel showed whatever Blender baked in — *picking 1×1 gave dark wood even with Light selected*. **Both varnishes already ship in `Panels-web.glb`** as real textured materials (`Pine Wood Light`/`pine-wood-light` 41 KB, `Pine Wood Dark`/`pine-wood-dark` 154 KB) — there are no wood texture *files* in the repo (the CSS preview fakes it with gradients), so grain quality is texture sampling, nothing to hand-preserve. **The `.glb`'s per-config varnish split is an asset artifact, not intent** (light on `4x2V/4x2H/2x2/1x4V/1x4H`, dark on `1x1/2x1V/2x1H`): `build-web-glb.mjs` runs `prune()`, which deletes zero-user materials, so **assigning each varnish to ≥1 panel is the only thing keeping both textures in the 319 KB web build** — never set every panel to one varnish in Blender or the other silently disappears and the toggle breaks. **Implementation:** `woodMats{light,dark}` are captured during the `loadModel` traverse *before* the per-mesh `child.material.clone()`, which is the last moment the two shared originals are reachable; `applyWoodVarnish()` assigns the active one to `Frame` + `Back Support` (`WOOD_COMPONENTS` — same physical wood, always one varnish) across all `PANEL_KEYS`. Sharing one material across wood meshes is safe because nothing mutates them (only the fabric front's material is touched, by the art/hover paths). `setVarnish()` on `window.audial3D` is called from the `.fopt[data-option="wood"]` handler; the render loop is continuous so the swap lands next frame — no `showConfig` re-run. **Seeded at the `showForSize` choke point, not per call site** — that one function covers size pick, orientation swap, and `loadPanelToEditor`, so an edited saved panel restores its varnish (the DEV-34 `__artImageEl` gap, closed structurally instead of by sprinkling calls). `currentVarnish` defaults to `'light'`, already agreeing with `freshPanel()` — unlike the `currentFold='full'` vs `fabricWrap='half'` mismatch DEV-36 still has to fix. **Asset note:** `Pine Wood Dark` roughness went 0.50 → 1.00 to match Light (both matte). In Blender "zero gloss" = Roughness **1.0**, not 0 — 0 is a mirror. **Follow-up — Finish section un-gated:** the Finish section (`#customSection` — a misnomer: it holds Wood + Side Wrap, nothing to do with custom sizes) only appeared *after* an image upload, hiding the varnish buttons until art existed. Never a principled gate — `display:block` was set in `handleImageUpload`'s `img.onload`, but the Clear-art handler never hid it again, so Finish already survived clearing the artwork. Now revealed in `showDesigner()`, the single entry both the catalog-size and custom-size flows funnel through (`loadPanelToEditor` shows it itself and skips `showDesigner`, so it's unaffected); the redundant upload-time line is gone. Finish now sits above the DES-8 Image Tips card and the two coexist until upload hides the tips. (`configurator.html`)

- **Components drawer + artwork on fabric (DEV-33, DEV-34)** ✓ — The configurator's Three.js preview is now a working designer, not just a viewer. **Orientation drives the model:** `SIZE_TO_CONFIG` maps each non-square size to a `{horizontal,vertical}` pair; `configKeyFor(size,orientation)` resolves it and `activeConfigKey()` is the single lookup for "which config is on screen" — all 8 `.glb` configs are reachable (previously only 5, and a vertical flip left a tall CSS face mirroring onto a horizontal quad). **Components drawer** (`.components-slideout` = vertical tab + 220px drawer riding one transform; parks outside `#panelContainer`'s `overflow:hidden`): 7 rows — the 6 mesh layers off `layerState`, which `showConfig` consults so state survives a config swap, plus **Artwork**, which is a *texture* not a mesh and so feeds `renderFrontFaceCanvas`'s `hasArt` branch. The Artwork row is dual-state (Upload button until art exists, then a toggle), derived from the same `artLoaded()` check the painter uses so the two can't disagree. Fabric off greys it. **Desktop overlays; mobile pushes** — an overlay bottom sheet buried the panel it explains, so below 900px the sheet goes 2-up (~145px) and the canvas takes an explicit px height down to the drawer top with the camera re-framing. **The fabric is clickable** (`frontHit` raycast): bare → file picker, art → edit mode (orbit off, toolbar shown), off-face → exit; drag repositions the art; hover brightens. Art hidden via the Artwork toggle is inert. Toolbar/Replace/Clear all drive the 3D face; `#imgReplaceBtn` was added because the in-panel Replace sits inside the hidden `panelFace`. **Traps worth knowing:** `#viewer3dCanvas` is `inset:0`+`height:100%`, so a `bottom` offset is ignored *and* `height:auto` resolves to the canvas's drawing-buffer size — resizing it needs an explicit px height, and `resize()` measures the canvas, not the container. The classic script's `imgPos`/`imgZoom`/`currentPanel` are top-level `let`s → global **lexical**, not on `window`; the module reads them by bare name. (`configurator.html`)

- **Foundational Three.js preview in the configurator (DEV-32, step 1)** ✓ — Replaced the CSS 3D panel preview in `configurator.html` with the Three.js render (blank fabric, orbit + zoom) — a **pure visual swap**, nothing else wired yet (deliberately small; the prior all-at-once DEV-32 attempt sprawled and was reverted). **Two-file glb strategy:** `Panels.glb` (~59 MB Blender master) is untracked + gitignored — its weight is two baked placeholder-artwork PNGs (47 MB @ 14400×7200 + 10.7 MB @ 8250×8250) vs. only ~1010 tris of geometry. `design-references/assets/3d-models/build-web-glb.mjs` (gltf-transform `prune`+`dedup`, `keepAttributes:true` to preserve fabric UVs) strips those PNGs + the Backdrop and reassigns all fabric meshes to one blank `Fabric` material, producing the committed **`Panels-web.glb` (~319 KB)** — the only model the site loads. Re-run `node build-web-glb.mjs --force` after any Blender re-export. **Viewer:** an isolated `<script type="module">` in `configurator.html` (patterns reused from the untouched `panel-viewer.html`; three.js v0.160.0 via unpkg CDN) renders into `#viewer3dCanvas` inside `#panelContainer` and exposes `window.audial3D = { showForSize, show3D, available }`. Hidden until a size is picked: the size-card handler calls `showForSize(size)`, mapping `SIZE_TO_CONFIG` (1×1→`1x1`, 2×1→`2x1H`, 2×2→`2x2`, 4×2→`4x2H`, 1×4→`1x4V`) and toggling `.view-3d` on the container (hides `#sceneWrap`). **Custom size stays on the CSS preview.** `window.__viewer3dActive` makes the CSS-panel `pointerdown` drag handler yield to OrbitControls. **WebGL-unsupported or model-load-failure → CSS preview** (`#sceneWrap` is hidden, never deleted). **Fabric-invisible fix:** the generated blank `Fabric` material was single-sided while the visible fabric surface is a back-face → back-face-culled invisible; set `doubleSided:true` to match every other material (all master materials are double-sided). **Deferred to DEV-33+:** image upload onto the panel, wood varnish, fabric wrap, the components peek-inside slider, and the orientation toggle driving the model (each size loads one default orientation). `panel-viewer.html` untouched. (`configurator.html`, `design-references/assets/3d-models/build-web-glb.mjs`, `Panels-web.glb`)

- **Cinematic assembly animation for 3D panel viewer (DEV-31)** ✓ — Rebuilt `playAssemblyAnimation()` in the standalone Three.js viewer (`design-references/assets/3d-models/panel-viewer.html`) as a 4-phase cinematic sequence: (1) camera eases to a per-panel cached "home" transform (`homeCameraCache`, `tweenCamera`, 800ms ease-in-out-cubic); (2) all layers hidden, rotation zeroed; (3) each of the 6 layers reveals in physical build order (Frame → Rockwool → Fiberglass Sheet → Back Support → Fiberglass Screen → Acoustic Fabric) — panel rotates around its own vertical centerline (`panelPivot` at world origin, `tweenModelRotationY`), then layer fades opacity 0→1 (`tweenLayerFadeIn`), then holds 800ms; (4) OrbitControls, size buttons, and toggles re-enable (`setControlsDisabled` + `isAnimating` guard). Three load-time fixes: baked 180° base rotation (`model.rotation.y = Math.PI`) so the front/artwork side faces the camera; back layers swing full 180° (`BACK_SWING_ANGLE = Math.PI`); per-layer material cloning (`child.material.clone()`) so fades don't blink the shared-wood Frame. **Follow-up tweak:** near-square 2×2 loaded larger than the elongated panels under max-dimension fitting (`distance = maxDim * 2.5`); added `PANEL_DISTANCE_FACTOR = { '2x2': 1.45 }` applied in both `centerCameraOn` and `computeHomeCameraTransform` to pull its camera back so all 4 sizes load at comparable size. Standalone/internal viewer — not yet wired into the configurator (that's DEV-32). **Follow-up fix (8 configs, committed `7f56384`):** the `.glb` was later re-exported with 8 configs but the viewer still listed only 4 in `PANEL_KEYS`; the extra 4 (`1x4H`, `2x1V`, `2x1H`, `1x1`) were never classified/hidden and lingered `visible=true`, overlapping the selected panel (on size switch and during the animation). Fixed by listing all 8 configs + 4 new size buttons (relabeled `1×4` → `1×4 V`), building `panelMeshes` from `PANEL_KEYS`, giving square `1x1` the same `1.45` distance factor as `2x2`, and hiding any unclassified non-Backdrop mesh at load as defense-in-depth. Viewer now supports all 8 configs (4×2V/H, 2×2, 1×4V/H, 2×1V/H, 1×1), each shown in isolation. **Follow-up fix (invisible fabric + artwork):** the Acoustic Fabric layer — and the artwork, which is a texture *on* that mesh — never appeared on any panel. Node-name mismatch: the classifier's `COMPONENTS` list expected a node named exactly `"<key> Acoustic Fabric"`, but the `.glb` ships two per config — `"<key> Acoustic Fabric Half Fold"` and `"... Full Fold"` (the `fabricWrap: 'half'|'full'` variants). Neither matched, so `classifyMesh` returned `null` and the defense-in-depth line hid both fabric meshes on every panel. Fix: `classifyMesh` now regex-matches `^Acoustic Fabric (Half|Full) Fold$` and maps both to the single `Acoustic Fabric` component (tagging a `fold`); the loader stashes both in a `fabricFolds[key]` map; `applyFabricFold()` promotes the active fold into `panelMeshes[key]['Acoustic Fabric']` and force-hides the inactive one (no z-fight); a new "Fabric Fold" switch (Half | Full, default Full) drives it, disabled during the assembly animation. **Known asset gap:** only 4 of 8 configs (`4×2V`, `4×2H`, `2×2`, `1×4V`) have the artwork texture baked into the `.glb` — the other four show plain-grey fabric and the Artwork toggle is a no-op until the `.glb` is re-exported with those textures. (`design-references/assets/3d-models/panel-viewer.html`)

- **Website text content update (CON-30)** ✓ — Content-only copy update across three pages, sourced from `website-content/Website Content Update.docx`. **index.html:** hero heading "SOUND AS ART." → "Your Sound. Your Art." (restructured from 3-span to 2-span layout — `.word-as` dropped) and new sub-heading. **how-it-works.html:** hero description, "What They Do" section title ("No More Reflections. Just You & Your Sound"), Point 4 wording, misconception tile 01 body, and tile 03 header ("Partials only at high-frequencies."). **about.html:** hero title ("Customized Hand-Built Acoustic Panels"), full "Our Story" rewrite (heading + 3 paragraphs), and all 3 value point bodies (value 02 retitled "Orbed-Based Production" — transcribed as-is from source doc per founder confirmation, despite reading as a likely typo for "Order-Based"). No CSS/JS changes. (`index.html`, `how-it-works.html`, `about.html`)

- **8-step manufacturing timeline (DES-29)** ✓ — Expanded the How It Works process timeline from 7 to 8 steps by inserting a new Step 06 "Apply Fiberglass Screen" (grid-mesh SVG) between Back Support and Acoustic Cloth. Old steps 06/07 renumbered to 07/08. Heading updated to "Eight steps." and 8th mobile dot indicator added. SVG assets migrated from `7-steps/` to `8-steps/` folder. (`how-it-works.html`, `.process-track`, `#processDots`)

- **3D panel preview rotation defaults (DES-28)** ✓ — Increased default rotation on the 3D panel preview from `rotateY(-15deg) rotateX(5deg)` to `rotateY(-35deg) rotateX(8deg)` so the left and right wood frame edges are clearly visible. JS also updated to fix drag clamping and reset rotation to new defaults on per-size selection. (`configurator.html`, `.panel-3d`, drag handler)

- **3D panel preview anti-aliasing (DES-27)** ✓ — Reduced jagged/pixelated edges on the CSS 3D panel preview in configurator. `.panel-side` (wood strips): added `outline:1px solid transparent` + `will-change:transform` for GPU layer promotion. `.panel-face`: added `outline:1px solid transparent`. `.scene-wrap`: added `transform:perspective(1800px)` (double-perspective trick). Each strip's inner edge (face-adjacent) gets a `1.5px solid var(--ink)` border to bridge the compositing seam invisibly; each outer edge gets a matching wood-colour border to soften the hard clip. Base `.panel-side` borders moved to explicit `.panel-side.left` rule so `.top`/`.bottom` don't inherit spurious end-of-strip borders. `backface-visibility:hidden` intentionally excluded from `.panel-side` (would hide the 90°-rotated strips). Some sub-pixel aliasing at oblique drag angles is a browser CSS 3D limit. (`configurator.html`, `.panel-side`, `.panel-face`, `.scene-wrap`)

- **Unified notification bands (DES-25)** ✓ — Made configurator `.plan-banner` and visualizer `.cart-banner` visually identical. Added peach-red (`#e26167`) mosaic `::before` tile pattern to configurator (matching existing visualizer pattern from DES-5). Changed visualizer mosaic tiles from navy to peach-red. Standardized both bands to `var(--paper)` background so pattern is visible. Standardized dismiss button to solid navy (`--ink` bg, `--paper` text) with solid hover (`--paper` bg, `--ink` text) — removed gradient hover from visualizer. (`configurator.html`, `room-visualizer.html`, `.plan-banner`, `.cart-banner`, `::before`)

- **Step-grid button swap & dividing lines (DEV-24)** ✓ — Swapped first two `.step-card` elements in homepage `.step-grid`: Design Panels is now step 01, Visualize Space is step 02. Fixed missing dividing lines by switching to shared-border pattern (`.step-grid` owns `border-top` + `border-left`, each `.step-card` owns `border-right` + `border-bottom`). Fixed border flicker caused by per-card `transform` during scroll-reveal creating competing GPU layers: moved `reveal` from individual cards to parent `.step-grid`, added staggered `nth-child` opacity delays (0s/0.12s/0.24s/0.36s). Narrowed `.reveal` transition from `all` to `opacity,transform` and changed end state to `transform:none`. (`index.html`, `.step-grid`, `.step-card`, `.reveal`)

- **Accent color rebrand + gradient hovers (DES-23)** ✓ — Replaced `--accent` from `#ecad49` (yellow) to `#e26167` (peach-red) across all 5 pages (135 usages). Hero heading on `index.html`: `.word-as` changed from `--primary-light` to `--primary` so "SOUND AS" is dark blue, "ART" is peach. Button/tile hover backgrounds changed from flat `background:var(--accent)` to `linear-gradient(135deg, var(--primary), var(--primary-light))` with `color:var(--paper)` — affects `.cta:hover`, `.action-btn.dup:hover`, `.cart-banner button:hover`, `.side-wall-label:hover`, `.dp-save:hover`. Primary light (`#007da6`) demoted from standalone color to gradient-only companion. (`index.html`, `configurator.html`, `room-visualizer.html`, `how-it-works.html`, `about.html`)

- **2×2 grid for "Sound That Works" on mobile (MOB-21)** ✓ — On mobile (≤560px), the 4 benefit cards in the "Sound That Works" section were stacking into a single column. Fix: changed `.benefits-grid` from `grid-template-columns:1fr` to `1fr 1fr` in the `@media(max-width:560px)` block, and removed the single-column border overrides (`.benefit{border-right:none}` and `.benefit:not(:last-child){border-bottom}`) so the tablet's 2-column border rules (`nth-child(2n)` removes right border, `nth-last-child(-n+2)` removes bottom border) apply correctly. Desktop and tablet layouts unchanged. (`how-it-works.html`, `.benefits-grid`, `@media(max-width:560px)`)

- **Tighter step spacing on mobile (MOB-20)** ✓ — On mobile (≤900px), the 7 process step blocks in the horizontal scroll timeline had excess side padding making them feel spread apart. Fix: added `padding:0 2px` to `.process-step` inside the `@media(max-width:900px)` block, reducing side padding from the base `8px` down to `2px`. Desktop unchanged. (`how-it-works.html`, `.process-step`, `@media(max-width:900px)`)
- **Image tips layout on mobile (MOB-15)** ✓ — On mobile (≤560px), the "Works well" and "Avoid" columns in the image tips sidebar card were stacking below the SVG diagram. Fix: added a `@media (max-width:560px)` block that turns `.size-tips-inner` into a `grid` with `grid-template-columns:auto 1fr` — diagram pinned to column 1, `.stips-cols` to column 2, `.stips-bonus` and `details.stips-general` span both columns. Bottom border removed from `.stips-diagram` on mobile. Desktop unchanged. (`configurator.html`, `.size-tips-inner`, `.stips-diagram`, `.stips-cols`, `@media (max-width:560px)`)

- **Horizontal CTA buttons on hero (MOB-14)** ✓ — On mobile (≤560px), the "Design Panels" and "Visualize Space" CTA buttons in the hero were stacking vertically. Root cause: `@media (max-width:560px)` set `.cta-group{flex-direction:column}` and swapped border logic to `border-bottom` dividers. Fix: removed those three overrides so the base horizontal flex layout (`display:flex`, `flex:1` per button, `border-right` vertical divider, `max-width:480px`) now applies at all widths. Desktop unchanged. (`index.html`, `.cta-group`, `.cta`, `@media (max-width:560px)`)

- **Compact panel size buttons (MOB-18)** ✓ — On mobile, the 5 size chips in the visualizer sidebar were in a 2-column grid, requiring scrolling to see all options. Fix: switched `.panel-sizes` to `display:flex;flex-wrap:nowrap` at `@media (max-width:560px)` so all 5 chips sit in one horizontal row. Tightened per-chip styles: `flex:1;min-width:0;padding:6px 4px;min-height:44px` (meets 44px touch target), `.size-name` 12px → 11px, `.size-price` 10px → 9px. Desktop 2-column grid unchanged. (`room-visualizer.html`, `.panel-sizes`, `.size-chip`, `@media (max-width:560px)`)

- **Horizontal wall thumbnails (MOB-17)** ✓ — On mobile (≤560px), the 3 inactive wall thumbnails (Left/Back/Right Wall) were stacking vertically. Root cause: `@media (max-width:560px)` overrode `.wall-thumbs` to `grid-template-columns:1fr`. Fix: removed that override so the desktop `repeat(3,1fr)` grid persists at mobile. Also tightened spacing in the same block: gap 8px → 4px, thumb padding `6px 8px` → `4px 5px`, label font 10px → 9px, meta font 8px → 7px so all three thumbnails fit comfortably on narrow screens. (`room-visualizer.html`, `.wall-thumbs`, `@media (max-width:560px)`)

- **Horizontal room presets & dimensions (MOB-16)** ✓ — On mobile (≤560px), the 4 preset cards were collapsing to a single-column stack and the 4 dimension inputs were stacking individually (8 rows total). Root cause: the 560px media query overrode `.preset-grid` to `grid-template-columns:1fr` and added `flex-direction:column` to `.dim-row`. Fix: removed those overrides — the desktop `repeat(2,1fr)` grid and horizontal `flex` rows now persist at mobile, giving a 2×2 preset grid and two 2-field rows for dimensions. Existing border selectors (`nth-child(2n)`, `nth-last-child(-n+2)`, `:last-child`) correctly handle both layouts without extra overrides. (`room-visualizer.html`, `@media (max-width:560px)`)

- **"Your Designs" grid glitch (MOB-19)** ✓ — On mobile, design cards shrunk while the "+" add button stretched wider. Root cause: `.designs-add-btn` and `.designs-filler` were missing `width:100%;min-width:0`, unlike `.designed-card` which already had both. Added to both selectors so all four grid slots are equally constrained. (`room-visualizer.html`, `.designs-add-btn`, `.designs-filler`)

- **Hamburger menu (MOB-13)** ✓ — Mobile navigation across all 5 pages. Three-line button in nav top-right, hidden on desktop (`display:none`), visible at `max-width:900px`. Animates to X on open (top/bottom spans rotate ±45°, middle fades). Mobile menu drops below nav bar full-width with `--ink` border-top, `--paper` bg, all 5 links vertical with 14px padding. Active page link highlighted in `--accent`. Closes on outside tap or link tap. Also unified `about.html` breakpoint from `600px` → `900px` to match all other pages. (`index.html`, `configurator.html`, `room-visualizer.html`, `how-it-works.html`, `about.html`, `.hamburger-btn`, `.mobile-menu`, `nav.nav-open`)

- **Custom size panel (DEV-12)** ✓ — Custom size chip in configurator now opens a floating pop-up (anchored above the card, `position:absolute` on `.size-grid`) with W/H steppers (1–8 ft, `−`/`+` buttons with disable-at-limits). Confirms into the full configurator experience — image upload, transforms, wood/wrap all work identically to catalog sizes. Preview uses fit-to-container scaling so extreme aspect ratios (8×1, 7×2) always fill the viewer. Pricing: ₹650/sq ft placeholder, stored on cart item as `price` field. `getPanelPrice(panel)` helper used in both `updateCart()` and `renderCheckoutCart()`. Checkout shows `"{W}×{H} ft Custom Panel"` with a `"Pricing confirmed within 24 hrs"` note. (`configurator.html`, `#customPickerPopup`, `getPanelPrice`, `updatePanelPreview`, `renderCartCardPreview`)

- **Hero slider (DES-9)** ✓ — Two-slide looping hero on `index.html`. Slide 1: three panel configurations (single 4×2, split 2×2 pair, triptych ×3) draw sequentially via `stroke-dashoffset` line-draw animation. Slide 2: 7-col × 5-row artwork grid (12 panels, p1–p12) zooms in from overhead and scatter-settles with spring easing. Crossfade: `1.2s ease-in-out` film dissolve. Dwell: 7.2s on slide 1, 4.9s on slide 2. Dots positioned `position:absolute; top:100%` on `.hero-right` (outside flex flow, below art area). Key layout fix: `.cfg` needs `min-height:0` so flex children can shrink below SVG intrinsic height; SVG uses `width:100%; height:100%` with `preserveAspectRatio="xMidYMid meet"` to letterbox within available space. Mobile: `.hero-right{height:auto}` + `.slide-wrapper{aspect-ratio:1.4;flex:none}`. (`index.html`, `.hero-right`, `.slide-wrapper`, `.slide-configs`, `.cfg`, `goHeroSlide()`)

- **Manufacturing process timeline (DES-10, DES-29)** ✓ — 8-step horizontal timeline section on `how-it-works.html`, placed directly below the hero and above the Benefits section. Steps: 01 Source Wood, 02 Create Frame, 03 Install Rockwool, 04 Fiberglass Backing, 05 Back Support, 06 Fiberglass Screen, 07 Acoustic Cloth, 08 Package & Ship. Each step: accent number (01–08), inline SVG illustration (80×80, `stroke:var(--ink)`), uppercase step name. SVGs inlined for CSS animation targeting. Scroll-triggered line-draw animation via `stroke-dasharray/dashoffset` with 150ms stagger per step. Mobile: `overflow-x:auto` + `scroll-snap-type:x mandatory`, 8 dot indicators that update on scroll. CSS cascade note: process mobile overrides must live in a separate `@media` block *after* the base process styles, not inside the shared `@media(max-width:900px)` block. (`how-it-works.html`, `.process`, `.process-track`, `.process-step`, `.step-icon`, `#processScroll`, `#processDots`)

- **Nav logo swap (DES-11)** ✓ — Replaced inline SVG placeholder with two-PNG combo: `1.png` (logo mark, 50px) + `4.png` (wordmark, 64px), side by side with `gap:8px` in `.logo`. Nav height auto-sizes to 64px. Both PNGs have `--paper` background so whitespace is invisible. CSS classes: `.brand-logo-mark` / `.brand-logo-text`. Footer "ACOUSTIC◆" references and page title updates deferred. (`index.html`, `configurator.html`, `room-visualizer.html`, `how-it-works.html`, `about.html`)

- **Contextual image tips (DES-8)** ✓ — Dynamic "Image tips" sidebar card in configurator. Appears on size selection, disappears on upload. Orientation-aware (2×1, 4×2, 1×4 each have horizontal + vertical tip sets). Shows aspect-ratio SVG diagram, Works well / Avoid columns, optional bonus tip, and collapsible general guidelines. (`configurator.html`, `SIZE_TIPS`, `showSizeTips`, `hideSizeTips`)

### Design/Brand (future)
1. **Logo refresh** — Waiting for new logo from designer friend. Current horizontal logo will be replaced.
2. **Three-tone color approach** — New gradient colors to be implemented when finalized. Keep current for now.
3. **Website layout revisit** — After logo and color finalization.


## Resolved bugs

- **4×2 panel preview glitch on first selection (DEV-13)** ✓ — `updatePanelPreview()` was called before `showDesigner()`. While `.designer` is `display:none`, `panelContainer.offsetWidth` returns 0 — the fallback 600×540 dimensions make the 4×2 panel render at 572px wide, overflowing a ~350px mobile container. Switching orientation and back fixed it because by then the designer was visible. Fix: call `showDesigner()` and `panelActions.classList.add('visible')` first, then `panelContainer.getBoundingClientRect()` to force a synchronous layout reflow, then `updatePanelPreview()`. Same reorder applied to the custom panel creation flow. (`configurator.html`, `updatePanelPreview`, size card click handler, custom panel `pickerCreate` handler)

- **Wall thumbnail palette fix (DES-7)** ✓ — Replaced off-palette `#D8D3C6` beige with `var(--line)` on `.thumb-surface-wrap` (the mini wall frame) and `.wall-ground` (the floor strip on the main canvas). Defined `--wall: var(--paper)` in `:root` so wall surfaces have an explicit background. Hover on `.wall-thumb` now uses a soft `rgba(9,61,83,0.05)` background tint + `--primary-light` border and text (architectural tab feel) instead of the full ink/paper flip used elsewhere. Count badge and meta text also shift to `--primary-light` on hover. (`room-visualizer.html`, `.wall-thumb`, `.thumb-surface-wrap`, `.wall-ground`, `--wall`)

- **Horizontal misconceptions tiles (DES-6)** ✓ — Replaced the vertical myth list in the `.myths` section with a 3-column `.misconceptions-grid`. Each `.misconception-tile` shows a large accent number, an italic dimmed misconception quote (38% opacity), a 1px divider, and a bold `--accent` reality headline + body copy. Hover: a paper-tint fill sweeps up from the tile bottom via `::before` (`rgba(242,242,249,0.07)`, `translateY(100%)→0`), misconception dims to 16%, reality brightens to full opacity, and an accent underline sweeps left-to-right under `.tile-reality-head` via `scaleX(0)→1`. Responsive: 2-col on tablet (tile 3 spans full width via `grid-column:1/-1`), 1-col on mobile. Semi-transparent paper borders (`rgba(242,242,249,0.2)`) used throughout instead of `--ink` (invisible on dark bg). (`how-it-works.html`, `.misconceptions-grid`, `.misconception-tile`, `.tile-reality-head`)

- **Notification band panel-mosaic pattern (DES-5)** ✓ — Band background changed from `--accent` (yellow) to `--paper`. A `::before` pseudo-element carries a 120×40px SVG tile (two rows of navy `--ink` rectangles in acoustic panel proportions) repeating horizontally. A `mask-image` gradient keeps the left 35% fully transparent (text area stays clean) and fades the pattern in to `opacity:0.70` at the right edge near the Dismiss button. Dismiss button hover updated from `--paper` (invisible on paper band) to `--accent` yellow. This treatment is saved as the standard pattern for all future website bands. (`room-visualizer.html`, `.cart-banner::before`)

- **"Your Designs" grid order & label** ✓ — Grid now fills column-first (top-left → bottom-left → top-right → bottom-right) by reordering `pageEls` to `[0, 2, 1, 3]` before appending to the CSS row-first grid. Add-button label reads "Click to Design Your First Panel" when cart is empty and switches to "Click to Design Your Next Panel" once any design exists. (`room-visualizer.html`, `renderDesignedPanels`)

- **Checkout flow** ✓ — Two-step checkout modal on both Configurator and Room Visualizer pages. "Checkout →" button in visualizer sidebar replaces "Clear All Panels"; "Checkout →" in configurator replaces "Request Quote →". Step 1: cart review (panel thumbnails, size, varnish, wrap, qty, subtotal from `acousticCart`). Step 2: full order form (name, email, phone, address, pincode, city, notes). Submits to `rohan270@gmail.com` via Formspree (`xbdbzvao`). Success state echoes the customer's email. (`configurator.html`, `room-visualizer.html`)

- **"Clear All Panels" button relocated** ✓ — Removed from sidebar "Next step" section (replaced by "Checkout →") and moved to the main canvas area directly below the "Select a panel size first" hint. Styled as `.clear-canvas-btn` — same 10px uppercase typographic treatment as the hint, `width:fit-content`, `margin-left:auto` so it sits right-aligned flush under the hint. Confirm prompt on click. (`room-visualizer.html`)

- **Placed panels lost on page return** ✓ — `acousticRoomPlan` was saved to localStorage on navigation but never read back on a fresh page load (only bfcache restores were handled). Added `loadRoomPlan()` which restores `state.panels` and room dimensions (including input field values) from localStorage on every page load, called before `loadDesignedCart()`. Also wired `saveRoomPlan()` to the "Your Designs" + button click so the plan is always persisted before navigating to the configurator. (`room-visualizer.html`, `loadRoomPlan`, `buildAddBtn`)

- **"Clear" artwork button in configurator** ✓ — A "Clear" button (trash icon + text) sits in the preview toolbar immediately left of "↺ View". It appears only when an image is loaded (upload or cart Edit flow) and disappears after use. Clicking performs a partial reset: clears the image, all transforms, and the file-info chips, then restores the upload prompt — while keeping panel size, orientation, wood/fabric selection, and the designer open. Implemented as `#clearArtGroup` / `#clearArtBtn` in `.ctrl-group-wrap`; visibility wired in `handleImageUpload`, `loadPanelToEditor`, and `resetCurrentPanel`. (`configurator.html`)

- **Mouse-driven wall resize** ✓ — Hovering near any of the 4 edges of the active wall surface shows a directional resize cursor (`ns-resize` top/bottom, `ew-resize` left/right) and a floating readout (e.g. `10.0 ft`). Dragging adjusts the room dimension in 0.1 ft steps (Shift = 0.5 ft), syncs the "Enter Exact Dimensions" input fields live, and respects min/max limits (width/length 6–40 ft, height 7–14 ft). Implementation: 4 invisible `.resize-handle` overlay divs straddling each wall edge, repositioned by `updateResizeHandles()` after every `sizeWallSurface()` call; drag math anchored to start px/ft ratio so precision stays linear. (`room-visualizer.html`, `createResizeHandles`, `updateResizeHandles`, `bigWallScene` pointer events)

- **"Your Designs" persistent add tile** ✓ — Empty state now shows 1 "+" tile in the first slot of the 2×2 grid (3 filler cells alongside it) instead of a full-grid spanning block. The add button and fillers use the same two-section structure (`.designed-card-preview` + `.designed-card-info`) as real design cards so all tiles are the same height. (`room-visualizer.html`, `renderDesignedPanels`, `.designs-add-btn`, `.designs-filler`)

- **Room visualizer decimal dimensions** ✓ — "Enter Exact Dimensions" inputs now accept one decimal place (e.g. 8.7 ft). Added `step="0.1"` to all three inputs; switched `parseInt` → `parseFloat` + `Math.round(...*10)/10` in `updateDimensions` so typed decimals are preserved. Total wall area also rounds to 1 decimal. (`room-visualizer.html`)

- **"Add Design" on default wall panels** ✓ — Hovering a default panel reveals an "Add Design" button. Clicking opens a floating configurator popup (blurred backdrop, no dimming) with the panel's size and orientation pre-selected. On Save, the artwork is merged directly onto the wall panel and the design is added to "Your Designs". Closing without saving prompts Save / Continue Editing / Discard if an image was uploaded. Also fixed: default panels placed from size chips were missing an `orientation` field, causing the popup to always default to horizontal. (`room-visualizer.html` + `configurator.html`)

- **"Your Designs" repositioned to sidebar** ✓ — Moved from horizontal scrollable strip below the wall canvas to a 2×2 paged grid in the right sidebar, between "Panel sizes" and "Your plan". Cards fill row-by-row in pages of 4; scrolling snaps one page (2 columns) at a time. Empty state shows a full-page `+` button linking to the configurator. (`room-visualizer.html`, `renderDesignedPanels` + CSS/HTML restructure)

- **Designed panels missing in visualizer** ✓ — Root cause: raw base64 images (4–8 MB each) silently exceeded localStorage's 5 MB cap; `saveCart()` swallowed the `QuotaExceededError` with an empty catch. Fix: compress images to max 1200px / JPEG 0.82 on upload (~200 KB each), and surface the error to the user if quota is ever hit again. (`configurator.html`, `handleImageUpload` + `saveCart`)

- **Image edge clipping on upload** ✓ — Root cause: `fitImageToPanel`, `recenterImage`, and `clampImagePosition` used `panelFace.offsetWidth/offsetHeight` (which includes the 1.5px border) to calculate fit scale, causing the image to be scaled 2px too large and clipped by `overflow: hidden`. Fix: switched to `clientWidth/clientHeight` (border excluded). (`configurator.html`)

- **Placement cursor lock in visualizer** ✓ — Root cause: after placing the last copy of a designed panel, placement mode was never exited and `updateArmedState()` wasn't called after each placement, so the crosshair cursor and hint text stayed active indefinitely. Fix: auto-exit placement mode when remaining count hits 0, allow clicking an active designed card to deselect even at 0 remaining (matching size-chip toggle behaviour), and call `updateArmedState()` after every placement. (`room-visualizer.html`)

- **Equal-distance alignment arrows** ✓ — Root cause: no equal-distance detection existed; only edge/center snapping was implemented. Fix: added equal-distance snap for all 3 configurations (sandwiched, rightmost, leftmost panel) on both X and Y axes. Shows double-headed arrows (↔/↕) in each equal gap rather than a full-wall line. Full-wall scan on every drag tick so all equal-gap runs across all panels are highlighted simultaneously. (`room-visualizer.html`)

- **"Start Another" scroll position** ✓ — Two compounding issues: (1) `updateCart()` expanding the cart section caused a layout reflow after `scrollTo({top:0})` fired, drifting the page down — fixed by wrapping the scroll in `requestAnimationFrame`; (2) sticky nav (56px) overlapped the designer section's top, hiding the orientation toggle — fixed by adding `scroll-margin-top:64px` to `.designer`. (`configurator.html`)

- **"Start Another" hides panel preview** ✓ — `resetCurrentPanel()` removed `active` from `.designer`, collapsing the live preview. Fix: save the current size before reset, then re-click the same size card to reactivate the designer. A `_skipDesignerScroll` flag suppresses `showDesigner()`'s built-in scroll so the page instead lands on `.size-section` (with `scroll-margin-top:64px` for nav clearance). (`configurator.html`)

- **"Your Panels" tile sizing glitch** ✓ — Root cause: `min-height: 180px` on `.cart-preview` let the 4×2 vertical tile (160px 3D content + 36px padding = 196px) grow taller than all other tiles (clamped at 180px), causing the flex row to stretch shorter cards 16px extra with no mechanism to pin the buttons to the bottom. Fix: replaced `min-height: 180px` with fixed `height: 196px` so all tiles share the same dividing border; added `margin-top: auto` to `.cart-actions-row` to pin Edit/Remove to the card bottom; removed the quantity +/− bar entirely. (`configurator.html`, `.cart-preview`, `.cart-actions-row`)

- **"Your Designs" right border** ✓ — Root cause: the grid used a gap-trick (`gap:1.5px; background:var(--ink)` on the page, `border:1.5px solid var(--ink)` on the outer container) which only gives tiles visual right/bottom borders via the container's outer border — individual tiles had `border:none`. Fix: switched to individual tile borders — container keeps `border-top` + `border-left` (the two sides no tile can own), every tile (`.designed-card`, `.designs-filler`, `.designs-add-btn`) gets `border-right:1.5px solid var(--ink); border-bottom:1.5px solid var(--ink)`. No double-borders; consistent with `size-chip` approach. (`room-visualizer.html`, `.designs-grid-outer`, `.designs-page`, `.designed-card`, `.designs-filler`, `.designs-add-btn`)

- **"Your Panels" tile click to preview** ✓ — Clicking the image preview area of a saved tile now loads it into the live 3D viewer without removing it from "Your Panels", so designs can be browsed freely. The panel stays in the cart; only the Edit button removes and opens for editing. (`configurator.html`, `updateCart` click handler on `.cart-preview`)

- **Horizontal logo sizing and background blending** ✓ — Three compounding issues: (1) CSS `height: 32px` too small; (2) `viewBox="0 0 1874.9 750"` had ~258 units of blank margin top/bottom so the actual content (y≈258–492) rendered at only ~10px; (3) Inkscape baked a `<g fill="#fff">` background rect into the SVG which appeared as a white rectangle in the nav (the `style="fill:#f2f2f9"` override was stripped when inlining). Fix: trimmed viewBox to `"0 258 1874.9 234"` (exact content bounds), set nav `padding: 0 32px`, set `height: 56px`, and removed the background rect group and its clipPath def. Applied to all 5 HTML files and the asset SVG.

- **Panel preview sizes** ✓ — All sizes now use per-size overrides in `updatePanelPreview()` instead of a single sqrt(area) formula. 1×1 and 2×1 share a fixed unit of `0.44 × refMaxLinear` px/ft (2×1 is 2 units on the long axis). 4×2 and 1×4 share `1.127 × refMaxLinear / 4` px/ft so their 4ft long sides render identically regardless of orientation. 2×2 uses the original proportional formula unchanged. The preview container is locked to a fixed height (`max-height:540px`) to prevent it from growing when tall/vertical panels are selected; any slight overflow is clipped cleanly by `overflow:hidden`. (`configurator.html`, `updatePanelPreview`, `.panel-3d-container`)

---

## Brand voice (for content)

- Sparse, technical, slightly philosophical
- Never cute, never marketing-speak
- Use real numbers: 7-day build, 100% rockwool density, etc.
- Hindi/Sanskrit grace where natural (e.g., "Audial", "Nada"), never forced
- Pair with English clarity — bilingual readers should feel at home, English-only readers should never feel locked out
- Use ₹ for prices, never $

---

## Audience segments (for future content)

Three distinct audiences map to different entry points:
1. **Home Studios** — musicians, producers recording at home
2. **Listening Rooms** — home theaters, DJ booths, dedicated music rooms
3. **Podcasters & Creators** — voice work, streaming, content creation

Bar/pub/commercial is a separate flow — different sales motion (B2B, larger orders, on-site consultation) — should live on a future "Commercial" page, not on consumer homepage.

---

## What's planned

Content rewrites are the bottleneck. In progress (by founder):
- Hero copy with origin story
- Audience-segmented entry points
- About page Carnatic music origin
- Real workshop photos to replace SVG mockups in hero showcase

Future features to consider (from past discussions):
- Equal-distance alignment arrows in visualizer
- Save/share room layout
- B2B Commercial page
- Custom-size flow (currently a placeholder alert)

---

## Conventions for working with Claude

- Always run a local dev server (`python3 -m http.server 8000`) so changes preview live
- Don't ask Claude to fix bugs in the first message of a session — give context first (read CLAUDE.md, walk through the affected page)
- Commit to git after each meaningful change so rollback is easy
- When adding new tokens (colors, fonts, sizes), update this file in the same session
- **Never push `master` unasked** — a push deploys (see Deployment), and finished work is sometimes held back on purpose while it's being fine-tuned
- **Adding any new asset? Add its `cp` line to `netlify.toml` in the same change**, or it 404s in production while working perfectly on localhost
- Never put anything secret in a tracked file just because it isn't linked from a page — the allowlist is what keeps it off the site, not obscurity
