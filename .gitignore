.vercel
.superpowers/
docs/superpowers/

# Netlify build output — assembled by netlify.toml's build command, never committed.
dist/

# 3D pipeline: the Blender master is huge and local-only; the web build is committed.
design-references/assets/3d-models/Panels.glb
design-references/assets/3d-models/node_modules/

# Local-only credential files — never commit. Also excluded from deploys via .vercelignore.
website/
# DEV-50: local secrets for `netlify dev` (Netlify's own env vars are marked secret and not readable locally).
.env
.env.*
# Netlify Functions dependencies (package.json at repo root) and CLI state.
node_modules/
.netlify/

# Local-only working material
website-content/
screenshots/
social-media/
design-references/wesite-banners/
